<button  class='add-to-cart tile-quick-view-btn plpToCart <isif condition="${product.isProductRestricted}">d-none</isif>' aria-label="Add to Cart" data-pid="${product.id}" ${!pdict.readyToOrder ? "disabled" : ""} ></button>