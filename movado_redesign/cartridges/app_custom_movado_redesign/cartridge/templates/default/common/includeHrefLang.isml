<isif condition="${pdict.relativeURL}">
    <link rel="alternate" hreflang="x-default" href="https://www.movado.com/en${pdict.relativeURL}"/>
    <link rel="alternate" hreflang="en" href="https://www.movado.com/en${pdict.relativeURL}"/>
    <link rel="alternate" hreflang="en-us" href="https://www.movado.com/en${pdict.relativeURL}"/>
    <link rel="alternate" hreflang="en-gb" href="https://www.movado.com/en${pdict.relativeURL}"/>
    <link rel="alternate" hreflang="fr" href="https://www.movado.com/fr${pdict.relativeURL}"/>
</isif>