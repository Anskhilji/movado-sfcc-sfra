<iscomment>Custom Start: Adding placeholder text in the input fields and Changing in html structure similar to mvmt design </iscomment>
<form action="${URLUtils.url('Account-EswCouponValidation')}" redirec-url="${URLUtils.https('Checkout-Begin')}" class="eswCouponValidation" method="POST" name="eswCouponValidation-form">
    <div class="form-group required">
        <label class="form-control-label" for="login-form-email">
            ${Resource.msg('label.input.login.email', 'login', null)}
        </label>
        <input type="email" id="esw-coupon-validation-form" class="form-control esw-email-validation" name="eswCouponValidation" value="${pdict.userName}">
        <div class="esw-invalid-feedback invalid-feedback"></div>
    </div>

    <div class="submit-form-section text-left">
        <input type="hidden" name="${pdict.csrf.tokenName}" value="${pdict.csrf.token}"/>
        <button type="submit" class="btn btn-block btn-primary">${Resource.msg('button.checkout.as.guest', 'checkout', null)}</button>
    </div>
</form>
<iscomment> Custom End </iscomment>
