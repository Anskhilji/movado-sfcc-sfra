<div class="dropdown-menu" role="menu" aria-hidden="true">
  <ul>
    <iscomment> <isloop items="${categoryItem.subCategories}" var="category"> </iscomment>
      <li class="dropdown-item gtm-customEvent" role="menuitem" data-gtm-tracking='${JSON.stringify(category.gtmNavVal)}'>
        <a href="${category.url}" role="button" class="dropdown-link">${category.name} usman</a>
      </li>
    <iscomment> </isloop> </iscomment>
  </ul>
</div>