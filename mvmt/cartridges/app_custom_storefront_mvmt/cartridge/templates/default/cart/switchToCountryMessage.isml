<isif condition="${!empty(pdict.countrySwitch)}">
    <div class="d-flex justify-content-center text-danger">
        <span>Please </span>
        <a class= "link pl-1 pr-1 text-info" href="${pdict.countrySwitch.absURL}"> switch sites </a>
        <span> to ship to ${pdict.countrySwitch.displayName}</span>
    </div>
</isif> 