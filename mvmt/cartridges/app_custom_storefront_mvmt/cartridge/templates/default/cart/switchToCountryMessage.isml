<isif condition="${!empty(pdict.countrySwitch)}">
    <div class="d-flex justify-content-center text-danger">
        <span>${dw.web.Resource.msgf('label.country.swithch.please.text', 'cart', null)}</span>
        <a class= "link pl-1 pr-1 text-info" href="${pdict.countrySwitch.absURL}">${dw.web.Resource.msgf('label.country.swithch.switch.site.text', 'cart', null)}</a>
        <span>${dw.web.Resource.msgf('label.country.swithch.ship.to.text', 'cart', null,  pdict.countrySwitch.displayName)}</span>
    </div>
</isif> 