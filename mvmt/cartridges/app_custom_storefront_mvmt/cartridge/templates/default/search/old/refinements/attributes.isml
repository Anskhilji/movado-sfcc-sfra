<ul class="values content">
    <isloop items="${refinement.values}" var="refinementValue">
        <isif condition="${refinementValue.type === 'color'}">
            <isinclude template="search/old/refinements/attributes/color"/>
        <iselseif condition="${refinementValue.type === 'size'}">
            <isinclude template="search/old/refinements/attributes/size"/>
        <iselse>
            <isinclude template="search/old/refinements/attributes/boolean"/>
        </isif>
    </isloop>
</ul>
